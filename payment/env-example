# Payment Service Configuration

# Server Configuration
PORT=8081
GIN_MODE=release

# LemonSqueezy Configuration
LSZ_API_KEY=your_lemonsqueezy_api_key_here
LSZ_STORE_ID=your_store_id_here
LSZ_WEBHOOK_SECRET=your_webhook_secret_here
LSZ_REDIRECT_URL=https://mayura.rocks/dashboard
LSZ_RECEIPT_URL=https://mayura.rocks/receipt

# Firebase Configuration
# Option 1: Use service account file path
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/firebase_service_account.json

# Option 2: Use service account JSON directly (alternative to file path)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project",...}

# DynamoDB Configuration
DYNAMO_TABLE=subscriptions
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# Optional: For local development with DynamoDB Local
# AWS_ENDPOINT_URL=http://localhost:8000

# LemonSqueezy Variant IDs (update these with your actual variant IDs)
# These are used in the code to map tiers to LemonSqueezy variants
# PLUS_VARIANT_ID=123456
# PRO_VARIANT_ID=123457 